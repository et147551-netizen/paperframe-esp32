# Build output and fetched dependencies
.pio/
managed_components/
__pycache__/

# Local build settings: Wi-Fi, SMB and Google Photos credentials, API tokens. Never committed.
platformio_local.ini

# Keys never belong in the repository. The firmware signing key lives in ~/.m5paper-keys/.
*.pem
*.key

# Written by PlatformIO IDE with this machine's absolute paths, and regenerated by it
.vscode/c_cpp_properties.json
.vscode/launch.json

# Reference clones: regenerable, and of mixed licences
refs/

# Personal photographs used as bench inputs (EXIF carries GPS). Never committed.
/sample/
